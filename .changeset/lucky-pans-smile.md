---
"@para/matrix-bridge": patch
---

Make the firehose consumer's PLC directory configurable via `PLC_URL`, so the bridge can resolve DIDs minted by a local dev-env instead of only the public plc.directory.
