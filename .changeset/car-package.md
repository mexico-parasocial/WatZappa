---
'@atproto/car': minor
---

New `@atproto/car` package: utilities for reading and writing AT Protocol CAR (Content Addressable aRchive) files, with stream-based `writeCarStream` / `readCar` APIs and multi-root support for permissioned repos.
