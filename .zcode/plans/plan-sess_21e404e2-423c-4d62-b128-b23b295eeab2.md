# Branch cleanup: merge what matters, delete the rest

## Merge priority order

**0. Settle `main` first** (it's the integration point and currently dirty)
- The uncommitted matrix-bridge "institutions" work (13 modified + 6 untracked files, incl. untracked `packages/car/`) stays — never discard. Run the matrix-bridge tests, fix anything trivially broken, then commit it on `main` (plus a changeset per AGENTS.md). If the work turns out half-finished, commit it as a clearly-labeled WIP commit so the tree is clean; do not block the branch cleanup on completing it.

**1. Merge `wip/bsync-subscription` → `main`** — highest priority: smallest (2 commits), freshest, already pushed. Backend wiring (data-plane index, dev-env bsync/chat). Build + run affected package tests, resolve any conflicts, merge, push.

**2. Merge `wip/ui-redesign` → `main`** — 3 commits: oauth-provider-ui redesign (shells, consent-form) + UI dep bumps (vite 8, swc, tanstack). Build + affected tests, merge on top of step 1, push.

## Deletions (after the merges land on `origin/main`)

- `git push origin --delete wip/bsync-subscription wip/ui-redesign` and delete the local counterparts (merged → fully recoverable from main's history).
- Delete `codex/permissioned-data-port` + remove its worktree `../WatZappa-permissioned-data` (branch is already an ancestor of main; verify with `git branch --merged` first — no commits lost).
- Delete local `pr-5158`, `pr-5099`, `pr5099-upstream`, `pr-4986` — stale upstream-PR review checkouts, not your work; any can be restored anytime with `gh pr checkout <number>`.

## Declutter the graph permanently (the "why do I see 300 branches" fix)

- Delete cached upstream refs: `git for-each-ref refs/remotes/upstream --format='%(refname:short)' | xargs git branch -rd` (keeps `upstream/main`).
- Restrict the upstream fetch refspec to main only so future fetches don't re-download 300 branches: `git config remote.upstream.fetch "+refs/heads/main:refs/remotes/upstream/main"`.
- Run `git fetch --prune origin` to sync origin's view.

## Verification

- `git branch -a` shows exactly: `main` (local+origin) + `upstream/main`.
- `git status` clean, `git log --oneline -5` shows the two wip merges + the institutions commit on main.
- Build passes from root (`pnpm run build:ts`) before pushing main.

Nothing is force-pushed and no commits are discarded — every deleted branch is either merged or reconstructable from upstream GitHub.